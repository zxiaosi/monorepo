{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app": "yarn workspace app run dev",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean && rimraf ./node_modules",
    "demo1": "yarn workspace demo1 run dev",
    "demo2": "yarn workspace demo2 run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "engines": {
    "node": "^20.18.0"
  }
}
